<?php
//Start session

	
	//Check whether the session variable SESS_MEMBER_ID is present or not
	if(!isset($id_session['id']) || (trim($id_session['id']) == '')) {
		
		
	}
        $id_session=$row['id'];

?>